package com.example.personregistry.view;
//Add necessary import

public class PersonView {
    // You *could* put UI element references here (if not using FXML directly)
    // and methods to update the UI, but for now, we'll handle it in the controller.
}